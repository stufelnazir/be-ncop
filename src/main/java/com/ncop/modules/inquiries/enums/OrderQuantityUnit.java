package com.ncop.modules.inquiries.enums;

/** The pack level at which the customer has placed the order quantity. */
public enum OrderQuantityUnit {
    TERTIARY,
    SECONDARY,
    MONO_BOX,
    STRIP,
    TABLET,
    JAR
}
