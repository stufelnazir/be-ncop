package com.ncop.auth;

/**
 * @deprecated Replaced by free-form String role names on the Role entity.
 * This enum is retained only as a reference and is no longer used.
 */
@Deprecated
public enum RoleName {
    ADMIN, SALES, QA, REGULATORY
}